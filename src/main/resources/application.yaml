spring:
  security:
    oauth2:
      client:
        registration:
          custom:
            client-id: ssoClient-1
            client-secret: ssoClientSecret-1
            scope: read,write
            authorization-grant-type: authorization_code
            redirect-uri: 
        provider:
          custom:
            authorization-uri: 
            token-uri: 
            user-info-uri: 
            jwk-set-uri:
  thymeleaf:
    cache: false
      